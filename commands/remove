#!/bin/sh

usage() {
	echo "usage: git template remove [<options>]"
	echo ""
	echo "remove template"
	echo ""
	echo "    -h, --help            help"
	exit 1
}

main() {
	# sanity checks
	local re='-+h(elp)?(\ |$)'; [[ "$@" =~ $re ]] && usage
	rm -rf $DEFINE_TEMPLATE_ROOTPATH
}

main "$@"